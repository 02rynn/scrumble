{
  "name": "scrumble",
  "homepage": "https://o2rynn.github.io/scrumble",
  "scripts": {
    "predeploy": "rm -rf dist && mkdir dist && cp index.html index.js dist/",
    "deploy": "gh-pages -d ."
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
